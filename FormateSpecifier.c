#include <stdio.h>

int main()
{
 /*
 format specifier % = defines and formats a type of data to be dipalyed

 %c = character;
 %s = String  or (array of character);
 %f = float;
 %lf = long float;
 %d = integer;
 %.1=decimal precision;
 %1 = minimum field width;
 %- =left alin;

 */

 return 0;
}